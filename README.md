# Vue3  
**Stack:** Vue 3 + TypeScript + Pinia + Vue Router + Vite

Проект состоит из трёх частей:

1. **A — SPA-приложение** c таблицей пользователей, поиском и детализацией.  
2. **B — Рефакторинг JQuery и Тесты:** unit-тестирование утилиты форматирования суммы.  
3. **C — Виджет:** сборка основного приложения в формате **UMD** для подключения на сторонние сайты.

## 📑 Требования окружения

```bash
Node.js version used: **22.12.0**  
npm version: **10.9.0**
```

## 🔧 Установка

```bash
npm install
```

## ▶️ Запуск SPA

```bash
npm run dev
```

Приложение будет доступно по адресу:

```bash
http://localhost:5173
```

### Функциональность

1. Загрузка списка пользователей с JSONPlaceholder
2. Кеширование в localStorage
3. Поиск пользователей по имени/email
4. Таблица со строками, по клику — переход на UserDetailsPage
5. Детальная страница с информацией о пользователе
6. Возможность редактирования и сохранения обновлённого пользователя (локально)
7. Роутинг (hash-history или browser-history в зависимости от окружения)

## 🧪 Рефакторинг JQuery

Запуск тестов

```bash
npm run test
```

### Покрытие

В тестируемых утилитах:

1. проверка форматирования суммы (formatAmount)
2. корректность деления на 100
3. округление до двух знаков
4. поддержка разных валютных кодов
5. edge cases (0, маленькие значения)

Файл тестов:

```bash
tests/formatAmount.test.ts
```

## 📦 Виджет
Виджет собирается как UMD библиотека, которую можно подключить на любую страницу.

🔨 Сборка виджета

```bash
npm run build
```

После сборки в папке dist/ появится:

```bash
test-nomad-vue3.umd.js   ← основной виджет
users-widget.css      ← стили
```

### 🌐 Использование виджета на сторонней странице

Создайте простой demo.html:

```bash
<!doctype html>
<html>
  <head>
    <meta charset="utf-8"/>
  </head>
  <body>
    <div id="app"></div>
    <link rel="stylesheet" href="./dist/test-nomad-vue3.css">
    <script src="https://unpkg.com/vue@3"></script>
    <script src="./dist/users-widget.umd.js"></script>

    <script>
      if (window.UsersWidget && typeof window.UsersWidget.init === 'function') {
        window.UsersWidget.init('#app');
      } else {
        console.error('UsersWidget.init is not available', window.UsersWidget);
      }
    </script>
  </body>
</html>

```

## 🧱 Структура проекта

```bash
src/
  components/
  pages/
  router/
  store/
  utils/
  tests/
  types/
  widget.ts             ← входная точка виджета
  App.vue
  main.ts
  style.css             ← глобальные стили
vite.config.ts
```


